# Database Configuration
database:
  host: localhost
  port: 3306
  name: payment_db
  username: dev_user
  # Fixed indentation so password aligns with other database properties
  password: dev_pass123
  connection-pool:
    max-size: 10
    min-size: 2

# API Configuration
api:
  base-url: http://localhost:8081
  timeout: 30000
  # Quoted to safely preserve special characters in YAML parsing
  api-key: "test-key-@#$%"